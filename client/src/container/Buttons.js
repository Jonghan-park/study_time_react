import React from "react";
import Button from "../components/Button";

const Buttons = () => {
  return (
    <div>
      <Button name="Start" />
      <Button name="Stop" />
      <Button name="Reset" />
      <Button name="Save" />
    </div>
  );
};

export default Buttons;
